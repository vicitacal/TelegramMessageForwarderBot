# Bot API (required): get token from @BotFather
Telegram.BotToken=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Optional: bot owner Telegram user ID; only this user (and users added via /users add) can use the bot. If unset, the first user who sends /start becomes the owner.
# Telegram.BotOwnerId=123456789

# User account / MTProto (required for receiving from groups): get from https://my.telegram.org
Telegram.ApiId=12345678
Telegram.ApiHash=your_api_hash_here
Telegram.PhoneNumber=+1234567890

# Optional: only needed during first login
# Telegram.VerificationCode=12345
# Telegram.Password=your_2fa_password

# Optional: session file path (defaults to WTelegram.session in working directory if not set)
# Telegram.SessionPathname=./WTelegram.session
